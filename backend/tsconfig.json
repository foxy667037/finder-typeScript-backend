{
  "compilerOptions": {
    "target": "ES2024",                                  /* Set the JavaScript language version for emitted JavaScript. */
    "module": "NodeNext",                                /* Specify the module system for Node.js projects. */
    "rootDir": "./src",                                  /* Specify the root folder for source files. */
    "outDir": "./dist",                                  /* Specify the output folder for compiled files. */
    "esModuleInterop": true,                             /* Enables compatibility with CommonJS modules. */
    "forceConsistentCasingInFileNames": true,            /* Ensure correct casing in imports. */
    "strict": true,                                      /* Enable all strict type-checking options. */
    "skipLibCheck": true,                             /* Skip type checking of declaration files. */
    "noEmit": true,
    "allowImportingTsExtensions": true,
  },
  "include": [
    "src/**/*"                                           /* Include all TypeScript source files. */
  ],
  "exclude": [
    "node_modules"                                       /* Exclude the node_modules directory. */
  ]
}